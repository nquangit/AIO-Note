- Exported Receiver: Can be interacted with by any app on the device
- Executes arbitrary commands:
	- ProcessBuilder()
	- Runtime.exec()
	- Native code: system()
- Privilege Escalation: App runs as system UID and thus run the commands as this privileged UID

